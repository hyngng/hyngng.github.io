# SEO

## 요구 사항

- `robots.txt`, `sitemap.xml`, `RSS.xml`, `Atom.xml`을 제공한다.
- `<head>` 안에 페이지별 메타 태그를 넣을 수 있어야 한다.

## 확인이 필요한 점

- og:image를 모든 글에 강제할지 정해야 한다.
- Google Analytics를 전면 적용할지, 선택적으로 둘지 정해야 한다.
- canonical과 alternate 메타를 모든 언어 버전에 정확히 넣을지 정해야 한다.