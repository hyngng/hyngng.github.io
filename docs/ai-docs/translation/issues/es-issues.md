# Issues detectados en traducciones al español

Cada issue indica el par de archivos, la línea de la traducción al español (ESL) y la línea original coreana (KOL), seguido de una explicación y sugerencia de corrección.

---

## 1. `training-camp-logs.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 18 | 24 | Semántica | `aturdido` implica aturdimiento por un golpe o shock, mientras que `얼떨떨하다` describe una sensación de desconcierto/flotabilidad. → `desconcertado` / `aturdido (pero más suave)` |
| 22 | 28 | Omisión | `el propio plan de estudios para servicio complementario era más relajado que el del servicio activo` omite `분위기` (ambiente/atmósfera), que en KO forma parte del sujeto compuesto `교육과정과 분위기`. → `el propio plan de estudios y el ambiente del servicio complementario eran más relajados que los del servicio activo` |
| 47 | 85 | Metáfora perdida | `elementos comunes` traduce `공통분모` (común denominador) perdiendo la metáfora matemática del original. → `denominadores comunes` |
| 372 | 132 | Modalidad | `sería una distancia razonable` es una afirmación condicional directa, mientras que `~일지도 모르지만` tiene un matiz dubitativo/tentativo (quizás). → `quizás sería una distancia razonable` |

## 2. `philosophical-reflections.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 22 | 22 | Registro | `propugna` es excesivamente formal/legalista. KO `인정하자는` es coloquial (vamos a reconocer/admitir). → `reconoce` / `aboga por reconocer` |
| 26 | 26 | Ambigüedad | `la naturaleza y el entorno` traduce `성질과 환경`. `naturaleza` en español puede entenderse como "mundo natural", mientras que `성질` significa "propiedad/característica". → `las cualidades y el entorno` |
| 70 | 70 | Matiz conceptual | `justificación` tiene una connotación más negativa que `명분` (causa legítima/principio justo). En el contexto de criticar al confucianismo como `명분` del poder, el español suena más acusatorio. → `fundamento moral` / `causa principal` |

## 3. `musics-for-work.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 18 | 18 | Modalidad | `no siempre ayuda` es más cauteloso que `도움이 되는 일은 아닙니다` (no es algo que ayude). KO es una negación general definitiva; ES introduce una excepción temporal. → `no es beneficioso para el trabajo cognitivo` o `no ayuda al trabajo cognitivo` |
| 28 | 30 | Estructura | `también sigo las canciones que ponen en sus transmisiones en vivo para crear listas de reproducción` usa `sigo` (follow) en lugar de `찾아` (buscar/encuentro). El matiz cambia de "buscar activamente" a "seguir pasivamente". → `busco las canciones que ponen... para crear listas de reproducción` |
| 72 | 90 | Precisión | Revisar si `inmersión` traduce `몰입` (flow/inmersión profunda) correctamente — `inmersión` es adecuado pero `몰입` tiene un matiz de absorción total (flow state). Aceptable pero perdería el concepto de "flow" de Mihály Csíkszentmihályi implícito en `몰입`. → considerar `estado de flujo` si el contexto lo permite |

## 4. `camus-in-goryeo.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 20 | 26 | Matiz | `concebido para plantear el sentido de la vida` — `plantear` significa "proponer/plantear un problema", mientras que KO `제시하기 위해` = para presentar/proveer. El sentido cambia de "presentar/proveer" a "cuestionar/plantear". → `concebido para ofrecer/proveer el sentido de la vida` |
| 26 | 34 | Imprecisión cultural | `Tierra Pura Budista` traduce `불국토사상`. `Tierra Pura` (Pure Land) es un concepto budista específico que se refiere al paraíso de Buda Amitabha, mientras que `불국토사상` es la idea de que la península coreana es la "tierra de Buda" (불국토). No son equivalentes exactos. → `la idea de Corea como Tierra de Buda (bulgukto)` |
| 63 | 79 | Estructura | `esperanzas místicas como el budismo y la geomancia` — KO dice `불교와 풍수리지 등 신비주의에 기반한 희망` (esperanza basada en el misticismo). ES sitúa el budismo y la geomancia como ejemplos de "esperanzas místicas", cuando KO los presenta como fuentes/fundamentos de la esperanza. → `esperanzas basadas en el misticismo, como el budismo y la geomaNcia` (corregir `geomaNcia` y notar `풍수리지` es typo por `풍수지리`) |

## 5. `sabok-logs.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 102 | 108 | Concepto cultural perdido | `relación de subordinación obligatoria por normativa` traduce `규정 내의 복종이 의무화된 갑을관계`. `갑을관계` es un término cultural coreano específico (relación contractual desigual A-B). ES pierde este concepto. → `relación vertical obligatoria por normativa (gap-eul)` |
| 114 | 120 | Omisión sensorial | `agua estancada` traduce `고인 물의 썩은 내` (agua estancada + olor a podrido). El componente olfativo (`썩은 내` = hedor a putrefacción) se pierde. → `agua estancada y podrida` |
| 129 | 120 | Modismo perdido | `sufrir de joven, disfrutar de viejo` traduce `젊어서는 고생이고 늙어서는 신선놀음`. `신선놀음` = juego/placer de un inmortal taoísta. ES lo reduce a `disfrutar de viejo`. → `sufrir de joven, disfrutar como un inmortal en la vejez` |

## 6. `thoughts-on-art-of-war.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 34 | 34 | Matiz | `cierta confianza asumida en la marca` — `asumida` (assumed) para `가정적인` (presuntivo/hipotético). Aunque cercano, `asumida` implica una suposición activa, mientras `가정적인` es más pasivo/teórico. → `cierta confianza hipotética en la marca` |
| 63 | 65 | Polaridad | `apenas he entendido` implica que entendió muy poco (negativo). KO `겨우 이해한` = apenas logró entender (logro con dificultad, positivo). El matiz cambia de "logro mínimo" a "casi nada". → `he logrado entender con dificultad` |
| - | - | Estructural | Las citas directas del texto coreano se reemplazan por resúmenes debido a una nota de copyright (ESL 18-20). Esto es una decisión editorial documentada, no un error de traducción, pero afecta la fidelidad del contenido. |

## 7. `thoughts-on-meditations.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 32 | 37 | Intensidad | `recelo` (recelo/sospecha leve) para `경계심` (vigilancia/actitud defensiva activa). `경계심` es más fuerte y activo que `recelo`. → `desconfianza` / `actitud defensiva` |
| 36 | 41 | Matiz semántico | `ligero peso de la vida humana` — `peso ligero` suena a una carga físicamente liviana. KO `가벼운 무게` (ligereza del ser) tiene connotación existencial/metafísica. → `la ligereza de la vida humana` |
| 82 | 91 | Precisión | Revisar si la traducción captura `명상록` como concepto unificado o si introduce fragmentación. → asegurar consistencia terminológica para `명상록` |

## 8. `thoughts-on-tao-te-ching.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 36 | 37 | Registro | `demasiado anticuada` para `너무 낡은 구닥다리`. `구닥다리` es coloquial/despectivo (cachivache/trasto viejo). ES es formal/neutro y pierde el tono coloquial desdeñoso. → `demasiado pasada de moda` / `un trasto demasiado anticuado` |
| 72 | 83 | Connotación | `utilizar` para `이용하라`. `이용하다` puede implicar "aprovecharse de" (connotación negativa), mientras `utilizar` es técnico/neutro. → `aprovechar` / `usar en beneficio propio` (si el contexto lo sugiere) |

## 9. `2023-movies-review.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 61 | 63 | Cargo | `director general del desarrollo de armas de destrucción masiva` para `대량살상무기 개발 총책임자`. `director general` es un título corporativo específico; `총책임자` = responsable máximo/jefe. → `máximo responsable del desarrollo de armas de destrucción masiva` |
| 91 | 103 | Matiz cultural | `responsable de la inspección de daños` para `검수 담당`. `검수` = inspección de calidad/verificación de entrega. `daños` (daños) es más limitado que `검수` (verificación completa). El contexto es una cadena de suministro/distribución. → `encargado de verificación de calidad` (si aplica al contexto) |

## 10. `look-back-movie-review.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 34 | 40 | Préstamo cultural | `sobria` para `시크한` (chic). `시크(chic)하다` = elegante con estilo/sofisticado. `Sobria` = austera/seria. El concepto fashion de "chic" se pierde. → `chic` / `con estilo` / `elegante` |
| 36 | 42 | Intensidad | `especial esmero` para `성심성의껏` (con toda sinceridad y devoción). `성심성의껏` es una expresión intensa de dedicación total. `especial esmero` (cuidado especial) es más débil. → `con extraordinario esmero y dedicación` |
| 40 | 46 | Matiz | Revisar la traducción de `인물에 대한 애정` (cariño/afecto hacia los personajes). Si se omite la noción de `애정`, se pierde la dimensión emocional del original. |

## 11. `ballerina-movie-review.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 29 | 33 | Semántica | `continúa excelentemente la serie` para `존 윅 시리즈를 훌륭히 계승한다`. `계승한다` = hereda/sucede (como legado). `Continúa` es simplemente "continúa". La noción de sucesión/herencia se pierde. → `hereda excelentemente la serie` / `continúa el legado de la serie` |
| 31 | 35 | Precisión | `continuación` para `계승` (sucesión). El término español `continuación` no comunica la idea de "hereda/carga con el legado". → `sucesión` / `herencia` (y ajustar `formales` → `técnicas` o similar) |
| 80 | 68 | Tono | KO `존 윅 유니버스 확장 그 자체에 집중해도 좋을 영화` tiene un tono aprobatorio (está bien/enfoque válido). Verificar que ES capte ese tono. |

## 12. `akg-n5-hybrid.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 39 | 59 | Omisión | `se ha beneficiado de ello` para `그 덕을 잘 보았는데`. KO incluye `잘` (bien) que intensifica el beneficio. ES pierde ese matiz de "sacar buen provecho". → `ha sabido aprovecharse bien de ello` |
| 46 | 74 | Perspectiva | `confianza en el sonido` (confianza del oyente hacia el sonido) para `소리에 대한 신뢰도` (confiabilidad/fiabilidad del sonido). Cambia la dirección de la confianza. → `la fiabilidad del sonido es suficiente` |
| 72 | 100 | Matiz | Revisar la traducción de `타협` (compromiso/concesión) — puede traducirse como `compromiso` pero en diseño de producto `타협` suele implicar trade-off más que compromiso. |

## 13. `sony-a7c-review.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 37 | 42 | Precisión | `bastante pesada y maciza` para `좀 묵직하고 무겁다`. `묵직하다` = sólido/consistente (sensación táctil positiva). `Maciza` = sólida/maciza. Adecuado, pero KO diferencia `묵직하다` (sólida) de `무겁다` (pesada). ES une ambos en una sola impresión. → `bastante sólida y pesada` (manteniendo separación) |
| 125 | 115 | Matiz | Revisar la traducción de `애매하다` (ambiguo/incierto) — puede traducirse como `ambiguo` o `difuso` según el contexto. Verificar consistencia. |

## 14. `fujifilm-x-e5-review.md`

| ESL | KOL | Tipo | Descripción |
|-----|-----|------|-------------|
| 58 | 56 | Préstamo cultural | `desenfadada` para `쿨한` (cool). `Desenfadada` = despreocupada/uninhibida. `Cool` = genial/tranquilo. El concepto de estilo "cool" no es exactamente `desenfadada`. → `una respuesta bastante cool` / `una respuesta muy tranquila` |
| 69 | 67 | Semántica | `emociones` para `감성` (sensibilidad/estética emocional). `감성` es más amplio que `emociones` — incluye apreciación estética y sensibilidad. → `con sensibilidad estética` / `con atractivo emocional` |
| 134 | 115 | Precisión | `algo más limitada que los modelos de gama alta` para `기함기 대비 좀 딸린다`. `딸린다` es coloquial y más fuerte (insuficiente/inferior). `limitada` es más suave. → `algo más limitada/inferior que los modelos de gama alta` |

---

**Total: 30 issues** distribuidos en 14 pares de archivos.
