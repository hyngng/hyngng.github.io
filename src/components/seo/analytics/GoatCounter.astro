---
import { SITE } from '../../../settings/site.settings';

const code = SITE.analytics.goatcounter.id;
if (!code || !import.meta.env.PROD) return;
---

<script
  is:inline
  data-goatcounter={`https://${code}.goatcounter.com/count`}
  src="//gc.zgo.at/count.js"
  async
></script>
